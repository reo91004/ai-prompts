# INDEX.md Entry Format

One line per work item, newest first:

```markdown
- [YYYY-MM-DD-L-slug](YYYY-MM-DD-L-slug/) — [in_progress | blocked | done] — [다음 단계 또는 완료 요약 한 구절]
```

`L`은 그날의 생성 순서 문자(A, B, C, …)다.
